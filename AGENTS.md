# AGENTS.md

This is a personal Immich fork. **All fork-specific guidance lives in [`fork/`](./fork). Read [`fork/agents.md`](./fork/agents.md) before making any changes.**

The upstream Immich code is preserved as-is so upstream merges stay clean. Don't add fork-only docs to the repo root, `docs/`, or anywhere else upstream owns — put them in [`fork/`](./fork).
